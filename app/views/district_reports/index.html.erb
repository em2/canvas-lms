<% content_for :page_title do -%>District Selection<% end -%>

<h1>View Reports for:</h1>

<table>
	<% @context.sub_accounts.active.each do |account| -%>
	  <tr>
	  	<td><%= link_to account.name, report_district_report_path(params[:report_id], account.id) %> </td>
	  </tr>
	<% end -%>
</table>
